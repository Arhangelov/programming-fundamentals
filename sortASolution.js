function sortNumbers (numOne, numTwo, numTree) {


    // До тук връща само най-голямото число
    // А трябва да връща ( 3 0 -2 )

    for (let i = 1; i <= 1; i++) {
        if (numOne > numTwo && numOne > numTree) {
            console.log (numOne);
        }
        
        if (numTwo > numOne && numTwo > numTree) {
            console.log (numTwo);
        }
    
        if (numTree > numOne && numTree > numTwo) {
            console.log (numTree);
        }
    }

    for (let j = 1; i <= 1; i++) {
        
    }
}
sortNumbers (-2, 3, 0)